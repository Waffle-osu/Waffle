{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Waffle",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "cwd": "${workspaceFolder}/build",
            "output": "build/Waffle.exe"
        }
    ]
}